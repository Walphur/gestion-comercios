/** WalTech Design System — espaciado y radios (múltiplos de 8px). */
export const SPACE = {
  xs: 8,
  sm: 16,
  md: 24,
  lg: 32,
  xl: 48,
} as const;

export const RADIUS = {
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
} as const;

/** Filas por página en tablas con paginación client-side. */
export const TABLE_PAGE_SIZE = 25;
