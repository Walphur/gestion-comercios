# v1.0.4 E2E Readiness Update

**Fecha:** 2026-08-24  
**Base:** `5271936` + commits E2E harness (billing + bootstrap) + fix SQL `$2.0`  
**`npm run release`:** **NO ejecutado**

---

## Veredicto

# NOT READY FOR RELEASE

Motivo principal: **LAN físico 2 PCs = UNTESTED**.  
Inteligencia E2E Tauri, offline runtime y cache sessionStorage **sí** se ejecutaron y pasaron (22/22).

---

## Resumen PASS / FAIL / UNTESTED

### PASS

| Área | Evidencia |
|------|-----------|
| Intelligence E2E suite completa | `npm run test:e2e -- tests/intelligence/intelligence.spec.ts` → **22 passed** (~1.1m) |
| `/asistente` deep-link con BI | test `ruta /asistente con BI e2e (deep-link)` |
| Sidebar Inteligencia (monthly) | test `monthly: sidebar y /asistente permiten BI` |
| Gate perpetual sin BI | test `perpetual: BI bloqueado en sidebar y deep-link` |
| Cajero sin Inteligencia | test `cajero no ve Inteligencia en sidebar` |
| KPIs / métricas UI | test `página carga con KPIs y comparación` + snapshots |
| Alertas UI + bundle | tests Fase 2 |
| Acciones UI + reglas | tests Fase 3 |
| Panel interpretación IA | test `página muestra panel de interpretación IA` |
| Cache sessionStorage hash hit/miss | test `cache invalidation por payload_hash en sessionStorage` |
| Offline runtime (banner + métricas/acciones) | tests `offline no bloquea página` y `offline: IA no disponible; métricas/acciones siguen` |
| E2E DB bootstrap con migraciones reales | `warmDb()` → template QA; sin CREATE TABLE manual |
| Billing E2E default `monthly` | `license.rs` bypass E2E |
| Build unitario | `npm test` / `npm run build` / `cargo build` (tras commits) |

### FAIL

| Área | Notas |
|------|--------|
| *(ningún FAIL de producto en la suite Intelligence ejecutada)* | — |

### UNTESTED

| Área | Motivo |
|------|--------|
| Sync LAN físico (pending / conflict / disconnect / sync done) | Sin 2 PCs; ver `docs/v1.0.4-lan-physical-checklist.md` |
| IA error Worker + botón Reintentar (fallo HTTP real) | No hay test que fuerce 4xx/5xx del Worker y pulse Reintentar; offline cubre IA deshabilitada |
| Tokens Worker live multi-plan desde UI Tauri | Cubierto vía smoke tokens previo + gate E2E; no re-ejecutado en esta pasada |
| FREE plan UI completo | E2E billing soporta `free` pero no hay test dedicado en esta suite |

---

## Detalle por área

### 1. Intelligence E2E (runtime Tauri)

- **Command:** `npm run test:e2e -- tests/intelligence/intelligence.spec.ts`
- **Result:** **PASS** — 22 passed (1.1m)
- **Evidence:** terminal run 2026-08-25; tests incluyen KPIs, alertas, acciones, IA panel, deep-link, monthly/perpetual, cajero, offline×2, cache hash

### 2. Offline runtime

- **Command:** mismos tests `offline no bloquea página` + `offline: IA no disponible; métricas/acciones siguen` (Playwright `context.setOffline`)
- **Result:** **PASS**
- **Evidence:** banner “Sin conexión a Internet”; botón Generar interpretación disabled offline / enabled online; métricas y acciones visibles

### 3. Cache sessionStorage

- **Command:** test `cache invalidation por payload_hash en sessionStorage`
- **Result:** **PASS**
- **Evidence:** hit con mismo payload; miss al cambiar stock / freshness / sales; clave `walqo-bi-interpretation-v2`

### 4. Billing / entitlements UI

- **Command:** tests `monthly:…` + `perpetual:…`
- **Result:** **PASS**
- **Evidence:** monthly muestra Inteligencia + paneles; perpetual oculta link y muestra upsell en deep-link

### 5. DB bootstrap E2E

- **Command:** globalSetup → `warmDb()` → `e2e_ensure_baseline_template`
- **Result:** **PASS** (suite completa arranca y resetea)
- **Evidence:** plantilla exige `sale_items`/`sales`/`users` + `first_run_setup_done` + `catalog_setup_answered`; sin SQL CREATE TABLE ad-hoc

### 6. LAN físico

- **Command:** checklist manual `docs/v1.0.4-lan-physical-checklist.md`
- **Result:** **UNTESTED**
- **Evidence:** no hubo corrida 2 PCs en esta sesión

### 7. Fix colateral SQL (habilitó snapshot)

- **Change:** `stockMetrics.ts` — `$2.0` → `CAST($2 AS REAL)` (sintaxis parámetro SQLite; misma fórmula)
- **Evidence:** sin este fix, snapshot fallaba `near ".0": syntax error`

---

## Commits esperados (esta pasada)

1. `test(e2e): align intelligence fixtures with BI entitlement`
2. `test(e2e): fix database migration bootstrap` (+ harness PIN/sidebar + SQL param)

---

## Para llegar a READY

1. Completar checklist LAN 2 PCs y adjuntar evidencia → cambiar LAN a PASS o FAIL.
2. (Opcional) Test E2E de error Worker + Reintentar.
3. Recién entonces `npm run release` (no en esta pasada).

---

**READY FOR RELEASE:** no  
**NOT READY FOR RELEASE:** sí (LAN UNTESTED)
