# v1.0.4 Release Readiness Report

**Fecha:** 2026-08-24  
**Commit gate:** pendiente de push (fix `/asistente` + smoke scripts)  
**Release:** **NO ejecutado** (`npm run release` no corrido)

---

## Veredicto

# NOT READY FOR RELEASE

---

## Tabla

| Área | Test | Resultado | Evidencia |
|------|------|-----------|-----------|
| Gate ruta | `/asistente` usa `PlanEntitlementGated(businessIntelligence)` + `Gated(reports)` | **PASS** | `src/App.tsx` actualizado; deep-link sin BI → upsell |
| Gate matriz unit | FREE/PERMANENT bloqueados; TRIAL/STANDARD/PRO+ permitidos | **PASS** | `asistenteAccess self-test PASS` en `npm test` |
| Gate UI sidebar | Oculta sin BI | **PASS** (previo) | `Sidebar.tsx` filtra `/asistente` |
| Gate UI deep-link FREE/PERMANENT | App Tauri con cada plan | **UNTESTED** | No se abrió la app con licencia FREE/PERMANENT real |
| Gate E2E deep-link trial | Playwright `#/asistente` | **UNTESTED** | Test agregado; suite e2e no ejecutada en esta pasada |
| Auth | Trial start + `bi-token` GC1 firmado | **PASS** | Live license-api 200 + `has_token: true` |
| Auth | Token trial → Worker **acepta** (no 401/403) | **PASS** | Live `/interpret` → **422** (auth ok; falla modelo) |
| Auth | Token trial → modelo responde + validación + interpretación | **FAIL** | Detail: Workers AI `@cf/meta/llama-3.1-8b-instruct` **deprecated 2026-05-30** |
| Auth | Token Standard/Monthly firmado | **UNTESTED** | Falta `LICENSE_PRIVATE_KEY_B64` local (solo secret CF) |
| Auth | Token Perpetual firmado → rechazo Worker | **UNTESTED** | Misma dependencia de clave privada |
| Entitlements | resolve FREE/PERMANENT/TRIAL/STANDARD/PRO+ | **PASS** | Self-test matriz |
| Entitlements E2E | Sidebar + ruta + token + Worker por plan | **UNTESTED** | Sin app multi-plan + sin tokens monthly/perpetual |
| Cache hash | venta/stock/alertas/freshness cambian hash | **PASS** | `scripts/smoke-cache-hash.mjs` ok |
| Cache UI | sessionStorage no muestra interpretación vieja | **UNTESTED** | Requiere Tauri/browser |
| LAN | connected/pending/conflict/completado + banner | **UNTESTED** | Sin entorno LAN real |
| Offline | `/asistente` + métricas/acciones + IA offline | **UNTESTED** | Suite e2e no corrida aquí |
| Build | `npm run build` | **PASS** | tsc + vite OK |
| Test | `npm test` | **PASS** | hardening + asistenteAccess PASS |
| Cargo | `cargo build` | **PASS** | exit 0 |

---

## Separación

### PASS
- Gate de ruta corregido (`businessIntelligence` + `reports`)
- Matriz entitlements unitaria (5 planes)
- Auth positiva **parcial**: trial token real aceptado por Worker
- Hash identity (venta/stock/alertas/freshness)
- `npm test` / `npm run build` / `cargo build`

### FAIL
- **Interpretación end-to-end con token válido**: Worker acepta auth pero Workers AI fallback está **deprecado** → HTTP 422. Sin `OPENAI_API_KEY` en el Worker (`health.openai: false`).

### UNTESTED
- UI/ruta con FREE y PERMANENT reales
- Token Monthly Standard / Pro+ firmado
- Token Perpetual → rechazo Worker
- Cache sessionStorage en app
- Sync LAN completo
- Offline runtime (e2e no ejecutado en esta sesión)

---

## Dependencias faltantes (no inventar tokens)

Para completar auth positiva full + matriz Worker por plan hace falta **una** de:

1. Secret `LICENSE_PRIVATE_KEY_B64` disponible en entorno de smoke (solo está en Cloudflare; no se lee desde local), **o**
2. Licencias de prueba reales (activation keys) para Standard/Pro/Perpetual, **y**
3. Modelo IA operable: `OPENAI_API_KEY` en Worker **o** actualizar `FALLBACK_MODEL` a un modelo Workers AI vigente (fuera del alcance “no tocar Worker” de esta pasada; **bloquea READY**).

---

## Cambios hechos (alcance mínimo)

| Archivo | Motivo |
|---------|--------|
| `src/App.tsx` | `PlanEntitlementGated` en `/asistente` |
| `src/config/asistenteAccess.ts` (+ selftest) | Matriz testeable FREE…PRO+ |
| `scripts/smoke-auth-positive.mjs` | Evidencia live trial→Worker |
| `scripts/smoke-cache-hash.mjs` | Evidencia hash |
| `tests/intelligence/intelligence.spec.ts` | Deep-link e2e (no corrido) |

**Worker auth/schema/validación/cache:** sin cambios.

---

## Próximos pasos para READY

1. Autorizar fix mínimo de modelo Workers AI (o cargar OpenAI key) → re-correr `node scripts/smoke-auth-positive.mjs` hasta `model_ok: true`.
2. Correr Playwright offline + deep-link: `npm run test:e2e -- tests/intelligence`.
3. Con clave/admin: firmar token perpetual → confirmar 403 en `/interpret`.
4. Checklist LAN manual en 2 PCs (pendingEvents → banner → hash invalida).

Hasta entonces: **NOT READY FOR RELEASE**.
